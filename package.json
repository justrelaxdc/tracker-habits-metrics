{
  "name": "obsidian-habit-notes",
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --target=es2020 --outfile=main.js --format=cjs --external:obsidian",
    "dev": "esbuild src/main.ts --bundle --target=es2020 --outfile=main.js --format=cjs --external:obsidian --watch"
  },
  "dependencies": {
    "chart.js": "^4.4.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "esbuild": "^0.23.0",
    "obsidian": "^1.5.0"
  }
}
